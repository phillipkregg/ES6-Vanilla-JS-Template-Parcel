const Header = () => {
  const template = `
        <header>
            <h1>My Parcel App</h1>
            <p>This is a boilerplate for simple js workflow</p>
        </header>
    `;

  return template;
};

export default Header;
